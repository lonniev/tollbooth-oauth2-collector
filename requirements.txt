# Pinned for reproducible deploys — the collector's framing (fastmcp) and its
# auth-code crypto (tollbooth-dpyc's encrypt_collector_code) must NOT drift on
# redeploy. tollbooth-dpyc was imported by server.py since v0.2.3 but was missing
# here, so the platform deployed without it → store_code raised
# "No module named 'tollbooth'" → codes never stored → every consumer's OAuth
# stranded on `pending`. Keep this in lockstep with what OAuth consumers pin.
fastmcp==3.1.0
httpx>=0.27.0
cryptography>=46.0.5
tollbooth-dpyc==0.63.2
